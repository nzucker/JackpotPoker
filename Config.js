window.Configuration = "" +
	"IsDebug = false;" +
	"IsSkipEndRoundAnimations = false;" +
	"IsHideSplashScreens = false;" +
	"IsHideNameSelection = false;" +
	"IsHideBuyInSelection = false;" +
	"IsHideWheel = false;" + 
	"IsSkipDices = false;" +

	// run hand compare test - you will see the result in the logs" +
	"IsCheckHandCompare = false;" +

	// Adds a robber OR joker to the first slots" +
	"IsForcedRobber = false;" +
	"IsForcedJoker = false;" +

	// Allows to set the player balance"
	"IsResetBalance = false;" +
	"NewBalance = 15;" + 
	
	// Choose a specific script (-1 = random)
	"ScriptNumber = -1;" +
	
	// Delay duration, after the wheel of fortune spin
	"DelayAfterSpin = 3";
	
	